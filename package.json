{
  "name": "telegram-render-bot",
  "version": "2.1.0",
  "description": "OpenClaw Gateway Telegram webhook untuk Render",
  "type": "module",
  "scripts": {
    "start": "node scripts/start-render.mjs",
    "check": "node --check scripts/ensure-openclaw-config.mjs && node --check scripts/render-webhook-proxy.mjs && node --check scripts/start-render.mjs"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "dependencies": {
    "openclaw": "2026.6.11"
  }
}
